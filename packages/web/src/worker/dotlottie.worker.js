/**
 * This is a worker script for dotlottie.
 * Do not modify this file directly.
 * As it's generated by the build process.
 */

var Gi = Object.defineProperty;
var Xi = (u, i, s) => (i in u ? Gi(u, i, { enumerable: !0, configurable: !0, writable: !0, value: s }) : (u[i] = s));
var T = (u, i, s) => Xi(u, typeof i != 'symbol' ? i + '' : i, s);
var nt = class {
    requestAnimationFrame(i) {
      return requestAnimationFrame(i);
    }
    cancelAnimationFrame(i) {
      cancelAnimationFrame(i);
    }
  },
  rt = class {
    constructor() {
      T(this, '_lastHandleId', 0);
      T(this, '_lastImmediate', null);
    }
    requestAnimationFrame(i) {
      return (
        this._lastHandleId >= Number.MAX_SAFE_INTEGER && (this._lastHandleId = 0),
        (this._lastHandleId += 1),
        (this._lastImmediate = setImmediate(() => {
          i(Date.now());
        })),
        this._lastHandleId
      );
    }
    cancelAnimationFrame(i) {
      this._lastImmediate && clearImmediate(this._lastImmediate);
    }
  },
  ke = class {
    constructor() {
      T(this, '_strategy');
      this._strategy = typeof requestAnimationFrame == 'function' ? new nt() : new rt();
    }
    requestAnimationFrame(i) {
      return this._strategy.requestAnimationFrame(i);
    }
    cancelAnimationFrame(i) {
      this._strategy.cancelAnimationFrame(i);
    }
  };
var it = typeof window < 'u' && typeof window.document < 'u';
var ye = {
  name: '@lottiefiles/dotlottie-web',
  version: '0.25.0',
  type: 'module',
  description: 'Lottie and DotLottie player for the web',
  repository: { type: 'git', url: 'git+https://github.com/LottieFiles/dotlottie-web.git', directory: 'packages/web' },
  homepage: 'https://github.com/LottieFiles/dotlottie-web#readme',
  bugs: 'https://github.com/LottieFiles/dotlottie-web/issues',
  author: 'LottieFiles',
  contributors: ['Abdelrahman Ashraf <a.theashraf@gmail.com>'],
  license: 'MIT',
  main: 'dist/index.cjs',
  module: 'dist/index.js',
  types: 'dist/index.d.ts',
  files: ['dist'],
  keywords: ['dotlottie', 'lottie', 'player', 'animation', 'web', 'canvas', 'javascript', 'thorvg'],
  scripts: {
    build: 'tsup',
    dev: 'tsup --watch',
    lint: 'eslint --fix .',
    'stats:eslint': 'cross-env TIMING=1 eslint .',
    'stats:ts': 'tsc -p tsconfig.build.json --extendedDiagnostics',
    test: 'vitest run --browser.headless',
    'test:coverage': 'vitest run --browser.headless --coverage',
    'test:watch': 'vitest',
    'type-check': 'tsc --noEmit',
  },
  devDependencies: {
    '@types/node': '^20.10.5',
    '@vitest/browser': '^1.2.2 ',
    '@vitest/coverage-istanbul': '^1.2.2',
    'cross-env': '7.0.3',
    tsup: '8.1.0',
    typescript: '5.0.4',
    vitest: '^1.2.2',
    webdriverio: '^8.32.0',
  },
  publishConfig: { access: 'public' },
};
var Ki = (() => {
    let u = typeof document < 'u' ? document.currentScript?.src : void 0;
    return function (i = {}) {
      let s = i,
        d,
        y,
        D = new Promise((e, t) => {
          (d = e), (y = t);
        }),
        Y = { ...s },
        lt = './this.program',
        ut = !0,
        H = '';
      function nn(e) {
        return s.locateFile ? s.locateFile(e, H) : H + e;
      }
      typeof document < 'u' && document.currentScript && (H = document.currentScript.src),
        u && (H = u),
        H.startsWith('blob:') ? (H = '') : (H = H.substr(0, H.replace(/[#?].*/, '').lastIndexOf('/') + 1)),
        (e, t, n) => {
          let r = new XMLHttpRequest();
          r.open('GET', e, !0),
            (r.responseType = 'arraybuffer'),
            (r.onload = () => {
              if (r.status == 200 || (r.status == 0 && r.response)) {
                t(r.response);
                return;
              }
              n();
            }),
            (r.onerror = n),
            r.send(null);
        };
      let on = s.print || console.log.bind(console),
        te = s.printErr || console.error.bind(console);
      Object.assign(s, Y),
        (Y = null),
        s.arguments && s.arguments,
        s.thisProgram && (lt = s.thisProgram),
        s.quit && s.quit;
      let ae;
      s.wasmBinary && (ae = s.wasmBinary);
      let be,
        dt = !1,
        B,
        F,
        ne,
        ce,
        A,
        C,
        ft,
        ht;
      function pt() {
        let e = be.buffer;
        (s.HEAP8 = B = new Int8Array(e)),
          (s.HEAP16 = ne = new Int16Array(e)),
          (s.HEAPU8 = F = new Uint8Array(e)),
          (s.HEAPU16 = ce = new Uint16Array(e)),
          (s.HEAP32 = A = new Int32Array(e)),
          (s.HEAPU32 = C = new Uint32Array(e)),
          (s.HEAPF32 = ft = new Float32Array(e)),
          (s.HEAPF64 = ht = new Float64Array(e));
      }
      let mt = [],
        _t = [],
        gt = [];
      function ln() {
        if (s.preRun)
          for (typeof s.preRun == 'function' && (s.preRun = [s.preRun]); s.preRun.length; ) fn(s.preRun.shift());
        Ue(mt);
      }
      function un() {
        Ue(_t);
      }
      function dn() {
        if (s.postRun)
          for (typeof s.postRun == 'function' && (s.postRun = [s.postRun]); s.postRun.length; ) pn(s.postRun.shift());
        Ue(gt);
      }
      function fn(e) {
        mt.unshift(e);
      }
      function hn(e) {
        _t.unshift(e);
      }
      function pn(e) {
        gt.unshift(e);
      }
      let J = 0,
        le = null;
      function mn(e) {
        J++, s.monitorRunDependencies?.(J);
      }
      function _n(e) {
        if ((J--, s.monitorRunDependencies?.(J), J == 0 && le)) {
          let t = le;
          (le = null), t();
        }
      }
      function Ce(e) {
        s.onAbort?.(e), (e = `Aborted(${e})`), te(e), (dt = !0), (e += '. Build with -sASSERTIONS for more info.');
        let t = new WebAssembly.RuntimeError(e);
        throw (y(t), t);
      }
      let gn = 'data:application/octet-stream;base64,',
        yt = (e) => e.startsWith(gn),
        re;
      (re = 'DotLottiePlayer.wasm'), yt(re) || (re = nn(re));
      function vt(e) {
        if (e == re && ae) return new Uint8Array(ae);
        throw 'both async and sync fetching of the wasm failed';
      }
      function yn(e) {
        return !ae && ut && typeof fetch == 'function'
          ? fetch(e, { credentials: 'same-origin' })
              .then((t) => {
                if (!t.ok) throw `failed to load wasm binary file at '${e}'`;
                return t.arrayBuffer();
              })
              .catch(() => vt(e))
          : Promise.resolve().then(() => vt(e));
      }
      function wt(e, t, n) {
        return yn(e)
          .then((r) => WebAssembly.instantiate(r, t))
          .then(n, (r) => {
            te(`failed to asynchronously prepare wasm: ${r}`), Ce(r);
          });
      }
      function vn(e, t, n, r) {
        return !e && typeof WebAssembly.instantiateStreaming == 'function' && !yt(t) && typeof fetch == 'function'
          ? fetch(t, { credentials: 'same-origin' }).then((o) =>
              WebAssembly.instantiateStreaming(o, n).then(r, function (l) {
                return (
                  te(`wasm streaming compile failed: ${l}`),
                  te('falling back to ArrayBuffer instantiation'),
                  wt(t, n, r)
                );
              }),
            )
          : wt(t, n, r);
      }
      function wn() {
        let e = { a: gi };
        function t(r, o) {
          return (b = r.exports), (be = b.ka), pt(), (Rt = b.oa), hn(b.la), _n(), b;
        }
        mn();
        function n(r) {
          t(r.instance);
        }
        if (s.instantiateWasm)
          try {
            return s.instantiateWasm(e, t);
          } catch (r) {
            te(`Module.instantiateWasm callback failed with error: ${r}`), y(r);
          }
        return vn(ae, re, e, n).catch(y), {};
      }
      var Ue = (e) => {
        for (; e.length > 0; ) e.shift()(s);
      };
      s.noExitRuntime || !0;
      let j = (e) => Nt(e),
        U = () => Gt(),
        bt = typeof TextDecoder < 'u' ? new TextDecoder('utf8') : void 0,
        Ct = (e, t, n) => {
          let r = t + n,
            o = t;
          for (; e[o] && !(o >= r); ) ++o;
          if (o - t > 16 && e.buffer && bt) return bt.decode(e.subarray(t, o));
          let a = '';
          for (; t < o; ) {
            let l = e[t++];
            if (!(l & 128)) {
              a += String.fromCharCode(l);
              continue;
            }
            let c = e[t++] & 63;
            if ((l & 224) == 192) {
              a += String.fromCharCode(((l & 31) << 6) | c);
              continue;
            }
            let f = e[t++] & 63;
            if (
              ((l & 240) == 224
                ? (l = ((l & 15) << 12) | (c << 6) | f)
                : (l = ((l & 7) << 18) | (c << 12) | (f << 6) | (e[t++] & 63)),
              l < 65536)
            )
              a += String.fromCharCode(l);
            else {
              let p = l - 65536;
              a += String.fromCharCode(55296 | (p >> 10), 56320 | (p & 1023));
            }
          }
          return a;
        },
        K = (e, t) => (e ? Ct(F, e, t) : ''),
        bn = (e, t, n, r) => {
          Ce(`Assertion failed: ${K(e)}, at: ${[t ? K(t) : 'unknown filename', n, r ? K(r) : 'unknown function']}`);
        },
        ie = 0;
      class Et {
        constructor(t) {
          (this.excPtr = t), (this.ptr = t - 24);
        }
        get_adjusted_ptr() {
          return C[(this.ptr + 16) >> 2];
        }
        get_caught() {
          return B[this.ptr + 12] != 0;
        }
        get_destructor() {
          return C[(this.ptr + 8) >> 2];
        }
        get_exception_ptr() {
          if (Jt(this.get_type())) return C[this.excPtr >> 2];
          let n = this.get_adjusted_ptr();
          return n !== 0 ? n : this.excPtr;
        }
        get_rethrown() {
          return B[this.ptr + 13] != 0;
        }
        get_type() {
          return C[(this.ptr + 4) >> 2];
        }
        set_adjusted_ptr(t) {
          C[(this.ptr + 16) >> 2] = t;
        }
        set_caught(t) {
          (t = t ? 1 : 0), (B[this.ptr + 12] = t);
        }
        set_destructor(t) {
          C[(this.ptr + 8) >> 2] = t;
        }
        set_rethrown(t) {
          (t = t ? 1 : 0), (B[this.ptr + 13] = t);
        }
        set_type(t) {
          C[(this.ptr + 4) >> 2] = t;
        }
        init(t, n) {
          this.set_adjusted_ptr(0), this.set_type(t), this.set_destructor(n);
        }
      }
      let Cn = (e) => {
          throw (ie || (ie = e), ie);
        },
        Ee = (e) => Yt(e),
        En = (e) => {
          let t = ie;
          if (!t) return Ee(0), 0;
          let n = new Et(t);
          n.set_adjusted_ptr(t);
          let r = n.get_type();
          if (!r) return Ee(0), t;
          for (let o in e) {
            let a = e[o];
            if (a === 0 || a === r) break;
            let l = n.ptr + 16;
            if (Xt(a, r, l)) return Ee(a), t;
          }
          return Ee(r), t;
        },
        Pn = () => En([]),
        Mn = (e, t, n) => {
          throw (new Et(e).init(t, n), (ie = e), ie);
        };
      function Tn(e, t, n) {
        return 0;
      }
      let Fn = (e, t) => {},
        Pt = (e) => {
          let t = 0;
          for (let n = 0; n < e.length; ++n) {
            let r = e.charCodeAt(n);
            r <= 127 ? t++ : r <= 2047 ? (t += 2) : r >= 55296 && r <= 57343 ? ((t += 4), ++n) : (t += 3);
          }
          return t;
        },
        $t = (e, t, n, r) => {
          if (!(r > 0)) return 0;
          let o = n,
            a = n + r - 1;
          for (let l = 0; l < e.length; ++l) {
            let c = e.charCodeAt(l);
            if (c >= 55296 && c <= 57343) {
              let f = e.charCodeAt(++l);
              c = (65536 + ((c & 1023) << 10)) | (f & 1023);
            }
            if (c <= 127) {
              if (n >= a) break;
              t[n++] = c;
            } else if (c <= 2047) {
              if (n + 1 >= a) break;
              (t[n++] = 192 | (c >> 6)), (t[n++] = 128 | (c & 63));
            } else if (c <= 65535) {
              if (n + 2 >= a) break;
              (t[n++] = 224 | (c >> 12)), (t[n++] = 128 | ((c >> 6) & 63)), (t[n++] = 128 | (c & 63));
            } else {
              if (n + 3 >= a) break;
              (t[n++] = 240 | (c >> 18)),
                (t[n++] = 128 | ((c >> 12) & 63)),
                (t[n++] = 128 | ((c >> 6) & 63)),
                (t[n++] = 128 | (c & 63));
            }
          }
          return (t[n] = 0), n - o;
        },
        Ln = (e, t, n) => $t(e, F, t, n),
        An = (e, t) => {};
      function In(e, t, n) {
        return 0;
      }
      let Sn = (e, t, n, r) => {};
      function kn(e, t, n, r) {}
      let Rn = (e, t) => {},
        Pe = {},
        We = (e) => {
          for (; e.length; ) {
            let t = e.pop();
            e.pop()(t);
          }
        };
      function ue(e) {
        return this.fromWireType(C[e >> 2]);
      }
      let se = {},
        q = {},
        $e = {},
        Mt,
        Me = (e) => {
          throw new Mt(e);
        },
        X = (e, t, n) => {
          e.forEach(function (c) {
            $e[c] = t;
          });
          function r(c) {
            let f = n(c);
            f.length !== e.length && Me('Mismatched type converter count');
            for (let p = 0; p < e.length; ++p) z(e[p], f[p]);
          }
          let o = new Array(t.length),
            a = [],
            l = 0;
          t.forEach((c, f) => {
            q.hasOwnProperty(c)
              ? (o[f] = q[c])
              : (a.push(c),
                se.hasOwnProperty(c) || (se[c] = []),
                se[c].push(() => {
                  (o[f] = q[c]), ++l, l === a.length && r(o);
                }));
          }),
            a.length === 0 && r(o);
        },
        xn = (e) => {
          let t = Pe[e];
          delete Pe[e];
          let n = t.rawConstructor,
            r = t.rawDestructor,
            o = t.fields,
            a = o.map((l) => l.getterReturnType).concat(o.map((l) => l.setterArgumentType));
          X([e], a, (l) => {
            let c = {};
            return (
              o.forEach((f, p) => {
                let m = f.fieldName,
                  w = l[p],
                  g = f.getter,
                  E = f.getterContext,
                  M = l[p + o.length],
                  O = f.setter,
                  I = f.setterContext;
                c[m] = {
                  read: (W) => w.fromWireType(g(E, W)),
                  write: (W, h) => {
                    let _ = [];
                    O(I, W, M.toWireType(_, h)), We(_);
                  },
                };
              }),
              [
                {
                  name: t.name,
                  fromWireType: (f) => {
                    let p = {};
                    for (let m in c) p[m] = c[m].read(f);
                    return r(f), p;
                  },
                  toWireType: (f, p) => {
                    for (var m in c) if (!(m in p)) throw new TypeError(`Missing field: "${m}"`);
                    let w = n();
                    for (m in c) c[m].write(w, p[m]);
                    return f !== null && f.push(r, w), w;
                  },
                  argPackAdvance: V,
                  readValueFromPointer: ue,
                  destructorFunction: r,
                },
              ]
            );
          });
        },
        Dn = (e, t, n, r, o) => {},
        jn = () => {
          let e = new Array(256);
          for (let t = 0; t < 256; ++t) e[t] = String.fromCharCode(t);
          Tt = e;
        },
        Tt,
        L = (e) => {
          let t = '',
            n = e;
          for (; F[n]; ) t += Tt[F[n++]];
          return t;
        },
        oe,
        v = (e) => {
          throw new oe(e);
        };
      function Un(e, t, n = {}) {
        let r = t.name;
        if ((e || v(`type "${r}" must have a positive integer typeid pointer`), q.hasOwnProperty(e))) {
          if (n.ignoreDuplicateRegistrations) return;
          v(`Cannot register type '${r}' twice`);
        }
        if (((q[e] = t), delete $e[e], se.hasOwnProperty(e))) {
          let o = se[e];
          delete se[e], o.forEach((a) => a());
        }
      }
      function z(e, t, n = {}) {
        if (!('argPackAdvance' in t)) throw new TypeError('registerType registeredInstance requires argPackAdvance');
        return Un(e, t, n);
      }
      var V = 8;
      let On = (e, t, n, r) => {
          (t = L(t)),
            z(e, {
              name: t,
              fromWireType(o) {
                return !!o;
              },
              toWireType(o, a) {
                return a ? n : r;
              },
              argPackAdvance: V,
              readValueFromPointer(o) {
                return this.fromWireType(F[o]);
              },
              destructorFunction: null,
            });
        },
        Wn = (e) => ({
          count: e.count,
          deleteScheduled: e.deleteScheduled,
          preservePointerOnDelete: e.preservePointerOnDelete,
          ptr: e.ptr,
          ptrType: e.ptrType,
          smartPtr: e.smartPtr,
          smartPtrType: e.smartPtrType,
        }),
        Be = (e) => {
          function t(n) {
            return n.$$.ptrType.registeredClass.name;
          }
          v(`${t(e)} instance already deleted`);
        },
        He = !1,
        Ft = (e) => {},
        Bn = (e) => {
          e.smartPtr ? e.smartPtrType.rawDestructor(e.smartPtr) : e.ptrType.registeredClass.rawDestructor(e.ptr);
        },
        Lt = (e) => {
          (e.count.value -= 1), e.count.value === 0 && Bn(e);
        };
      var At = (e, t, n) => {
        if (t === n) return e;
        if (n.baseClass === void 0) return null;
        let r = At(e, t, n.baseClass);
        return r === null ? null : n.downcast(r);
      };
      let It = {},
        Hn = () => Object.keys(he).length,
        zn = () => {
          let e = [];
          for (let t in he) he.hasOwnProperty(t) && e.push(he[t]);
          return e;
        },
        de = [],
        ze = () => {
          for (; de.length; ) {
            let e = de.pop();
            (e.$$.deleteScheduled = !1), e.delete();
          }
        },
        fe,
        Vn = (e) => {
          (fe = e), de.length && fe && fe(ze);
        },
        Yn = () => {
          (s.getInheritedInstanceCount = Hn),
            (s.getLiveInheritedInstances = zn),
            (s.flushPendingDeletes = ze),
            (s.setDelayFunction = Vn);
        };
      var he = {};
      let Nn = (e, t) => {
          for (t === void 0 && v('ptr should not be undefined'); e.baseClass; ) (t = e.upcast(t)), (e = e.baseClass);
          return t;
        },
        Gn = (e, t) => ((t = Nn(e, t)), he[t]),
        Te = (e, t) => {
          (!t.ptrType || !t.ptr) && Me('makeClassHandle requires ptr and ptrType');
          let n = !!t.smartPtrType,
            r = !!t.smartPtr;
          return (
            n !== r && Me('Both smartPtrType and smartPtr must be specified'),
            (t.count = { value: 1 }),
            pe(Object.create(e, { $$: { value: t, writable: !0 } }))
          );
        };
      function Xn(e) {
        let t = this.getPointee(e);
        if (!t) return this.destructor(e), null;
        let n = Gn(this.registeredClass, t);
        if (n !== void 0) {
          if (n.$$.count.value === 0) return (n.$$.ptr = t), (n.$$.smartPtr = e), n.clone();
          {
            let f = n.clone();
            return this.destructor(e), f;
          }
        }
        function r() {
          return this.isSmartPointer
            ? Te(this.registeredClass.instancePrototype, {
                ptrType: this.pointeeType,
                ptr: t,
                smartPtrType: this,
                smartPtr: e,
              })
            : Te(this.registeredClass.instancePrototype, { ptrType: this, ptr: e });
        }
        let o = this.registeredClass.getActualType(t),
          a = It[o];
        if (!a) return r.call(this);
        let l;
        this.isConst ? (l = a.constPointerType) : (l = a.pointerType);
        let c = At(t, this.registeredClass, l.registeredClass);
        return c === null
          ? r.call(this)
          : this.isSmartPointer
          ? Te(l.registeredClass.instancePrototype, { ptrType: l, ptr: c, smartPtrType: this, smartPtr: e })
          : Te(l.registeredClass.instancePrototype, { ptrType: l, ptr: c });
      }
      var pe = (e) =>
        typeof FinalizationRegistry > 'u'
          ? ((pe = (t) => t), e)
          : ((He = new FinalizationRegistry((t) => {
              Lt(t.$$);
            })),
            (pe = (t) => {
              let n = t.$$;
              if (!!n.smartPtr) {
                let o = { $$: n };
                He.register(t, o, t);
              }
              return t;
            }),
            (Ft = (t) => He.unregister(t)),
            pe(e));
      let Jn = () => {
        Object.assign(Fe.prototype, {
          isAliasOf(e) {
            if (!(this instanceof Fe) || !(e instanceof Fe)) return !1;
            let t = this.$$.ptrType.registeredClass,
              n = this.$$.ptr;
            e.$$ = e.$$;
            let r = e.$$.ptrType.registeredClass,
              o = e.$$.ptr;
            for (; t.baseClass; ) (n = t.upcast(n)), (t = t.baseClass);
            for (; r.baseClass; ) (o = r.upcast(o)), (r = r.baseClass);
            return t === r && n === o;
          },
          clone() {
            if ((this.$$.ptr || Be(this), this.$$.preservePointerOnDelete)) return (this.$$.count.value += 1), this;
            {
              let e = pe(Object.create(Object.getPrototypeOf(this), { $$: { value: Wn(this.$$) } }));
              return (e.$$.count.value += 1), (e.$$.deleteScheduled = !1), e;
            }
          },
          delete() {
            this.$$.ptr || Be(this),
              this.$$.deleteScheduled && !this.$$.preservePointerOnDelete && v('Object already scheduled for deletion'),
              Ft(this),
              Lt(this.$$),
              this.$$.preservePointerOnDelete || ((this.$$.smartPtr = void 0), (this.$$.ptr = void 0));
          },
          isDeleted() {
            return !this.$$.ptr;
          },
          deleteLater() {
            return (
              this.$$.ptr || Be(this),
              this.$$.deleteScheduled && !this.$$.preservePointerOnDelete && v('Object already scheduled for deletion'),
              de.push(this),
              de.length === 1 && fe && fe(ze),
              (this.$$.deleteScheduled = !0),
              this
            );
          },
        });
      };
      function Fe() {}
      let me = (e, t) => Object.defineProperty(t, 'name', { value: e }),
        St = (e, t, n) => {
          if (e[t].overloadTable === void 0) {
            let r = e[t];
            (e[t] = function (...o) {
              return (
                e[t].overloadTable.hasOwnProperty(o.length) ||
                  v(
                    `Function '${n}' called with an invalid number of arguments (${o.length}) - expects one of (${e[t].overloadTable})!`,
                  ),
                e[t].overloadTable[o.length].apply(this, o)
              );
            }),
              (e[t].overloadTable = []),
              (e[t].overloadTable[r.argCount] = r);
          }
        },
        Ve = (e, t, n) => {
          s.hasOwnProperty(e)
            ? ((n === void 0 || (s[e].overloadTable !== void 0 && s[e].overloadTable[n] !== void 0)) &&
                v(`Cannot register public name '${e}' twice`),
              St(s, e, e),
              s.hasOwnProperty(n) &&
                v(`Cannot register multiple overloads of a function with the same number of arguments (${n})!`),
              (s[e].overloadTable[n] = t))
            : ((s[e] = t), n !== void 0 && (s[e].numArguments = n));
        },
        Kn = 48,
        qn = 57,
        Qn = (e) => {
          if (e === void 0) return '_unknown';
          e = e.replace(/\W/g, '$');
          let t = e.charCodeAt(0);
          return t >= Kn && t <= qn ? `_${e}` : e;
        };
      function Zn(e, t, n, r, o, a, l, c) {
        (this.name = e),
          (this.constructor = t),
          (this.instancePrototype = n),
          (this.rawDestructor = r),
          (this.baseClass = o),
          (this.getActualType = a),
          (this.upcast = l),
          (this.downcast = c),
          (this.pureVirtualFunctions = []);
      }
      let Ye = (e, t, n) => {
        for (; t !== n; )
          t.upcast || v(`Expected null or instance of ${n.name}, got an instance of ${t.name}`),
            (e = t.upcast(e)),
            (t = t.baseClass);
        return e;
      };
      function er(e, t) {
        if (t === null) return this.isReference && v(`null is not a valid ${this.name}`), 0;
        t.$$ || v(`Cannot pass "${qe(t)}" as a ${this.name}`),
          t.$$.ptr || v(`Cannot pass deleted object as a pointer of type ${this.name}`);
        let n = t.$$.ptrType.registeredClass;
        return Ye(t.$$.ptr, n, this.registeredClass);
      }
      function tr(e, t) {
        let n;
        if (t === null)
          return (
            this.isReference && v(`null is not a valid ${this.name}`),
            this.isSmartPointer ? ((n = this.rawConstructor()), e !== null && e.push(this.rawDestructor, n), n) : 0
          );
        (!t || !t.$$) && v(`Cannot pass "${qe(t)}" as a ${this.name}`),
          t.$$.ptr || v(`Cannot pass deleted object as a pointer of type ${this.name}`),
          !this.isConst &&
            t.$$.ptrType.isConst &&
            v(
              `Cannot convert argument of type ${
                t.$$.smartPtrType ? t.$$.smartPtrType.name : t.$$.ptrType.name
              } to parameter type ${this.name}`,
            );
        let r = t.$$.ptrType.registeredClass;
        if (((n = Ye(t.$$.ptr, r, this.registeredClass)), this.isSmartPointer))
          switch (
            (t.$$.smartPtr === void 0 && v('Passing raw pointer to smart pointer is illegal'), this.sharingPolicy)
          ) {
            case 0:
              t.$$.smartPtrType === this
                ? (n = t.$$.smartPtr)
                : v(
                    `Cannot convert argument of type ${
                      t.$$.smartPtrType ? t.$$.smartPtrType.name : t.$$.ptrType.name
                    } to parameter type ${this.name}`,
                  );
              break;
            case 1:
              n = t.$$.smartPtr;
              break;
            case 2:
              if (t.$$.smartPtrType === this) n = t.$$.smartPtr;
              else {
                let o = t.clone();
                (n = this.rawShare(
                  n,
                  Q.toHandle(() => o.delete()),
                )),
                  e !== null && e.push(this.rawDestructor, n);
              }
              break;
            default:
              v('Unsupporting sharing policy');
          }
        return n;
      }
      function nr(e, t) {
        if (t === null) return this.isReference && v(`null is not a valid ${this.name}`), 0;
        t.$$ || v(`Cannot pass "${qe(t)}" as a ${this.name}`),
          t.$$.ptr || v(`Cannot pass deleted object as a pointer of type ${this.name}`),
          t.$$.ptrType.isConst &&
            v(`Cannot convert argument of type ${t.$$.ptrType.name} to parameter type ${this.name}`);
        let n = t.$$.ptrType.registeredClass;
        return Ye(t.$$.ptr, n, this.registeredClass);
      }
      let rr = () => {
        Object.assign(_e.prototype, {
          getPointee(e) {
            return this.rawGetPointee && (e = this.rawGetPointee(e)), e;
          },
          destructor(e) {
            this.rawDestructor?.(e);
          },
          argPackAdvance: V,
          readValueFromPointer: ue,
          fromWireType: Xn,
        });
      };
      function _e(e, t, n, r, o, a, l, c, f, p, m) {
        (this.name = e),
          (this.registeredClass = t),
          (this.isReference = n),
          (this.isConst = r),
          (this.isSmartPointer = o),
          (this.pointeeType = a),
          (this.sharingPolicy = l),
          (this.rawGetPointee = c),
          (this.rawConstructor = f),
          (this.rawShare = p),
          (this.rawDestructor = m),
          !o && t.baseClass === void 0
            ? r
              ? ((this.toWireType = er), (this.destructorFunction = null))
              : ((this.toWireType = nr), (this.destructorFunction = null))
            : (this.toWireType = tr);
      }
      let kt = (e, t, n) => {
          s.hasOwnProperty(e) || Me('Replacing nonexistent public symbol'),
            s[e].overloadTable !== void 0 && n !== void 0
              ? (s[e].overloadTable[n] = t)
              : ((s[e] = t), (s[e].argCount = n));
        },
        ir = (e, t, n) => {
          e = e.replace(/p/g, 'i');
          let r = s[`dynCall_${e}`];
          return r(t, ...n);
        },
        Rt,
        k = (e) => Rt.get(e),
        sr = (e, t, n = []) => (e.includes('j') ? ir(e, t, n) : k(t)(...n)),
        or =
          (e, t) =>
          (...n) =>
            sr(e, t, n),
        R = (e, t) => {
          e = L(e);
          function n() {
            return e.includes('j') ? or(e, t) : k(t);
          }
          let r = n();
          return typeof r != 'function' && v(`unknown function pointer with signature ${e}: ${t}`), r;
        },
        ar = (e, t) => {
          let n = me(t, function (r) {
            (this.name = t), (this.message = r);
            let o = new Error(r).stack;
            o !== void 0 &&
              (this.stack = `${this.toString()}
${o.replace(/^Error(:[^\n]*)?\n/, '')}`);
          });
          return (
            (n.prototype = Object.create(e.prototype)),
            (n.prototype.constructor = n),
            (n.prototype.toString = function () {
              return this.message === void 0 ? this.name : `${this.name}: ${this.message}`;
            }),
            n
          );
        },
        xt,
        Dt = (e) => {
          let t = Vt(e),
            n = L(t);
          return G(t), n;
        },
        Le = (e, t) => {
          let n = [],
            r = {};
          function o(a) {
            if (!r[a] && !q[a]) {
              if ($e[a]) {
                $e[a].forEach(o);
                return;
              }
              n.push(a), (r[a] = !0);
            }
          }
          throw (t.forEach(o), new xt(`${e}: ${n.map(Dt).join([', '])}`));
        },
        cr = (e, t, n, r, o, a, l, c, f, p, m, w, g) => {
          (m = L(m)), (a = R(o, a)), c && (c = R(l, c)), p && (p = R(f, p)), (g = R(w, g));
          let E = Qn(m);
          Ve(E, function () {
            Le(`Cannot construct ${m} due to unbound types`, [r]);
          }),
            X([e, t, n], r ? [r] : [], (M) => {
              var Se;
              M = M[0];
              let O, I;
              r ? ((O = M.registeredClass), (I = O.instancePrototype)) : (I = Fe.prototype);
              let W = me(m, function (...tt) {
                if (Object.getPrototypeOf(this) !== h) throw new oe(`Use 'new' to construct ${m}`);
                if (_.constructor_body === void 0) throw new oe(`${m} has no accessible constructor`);
                let Qt = _.constructor_body[tt.length];
                if (Qt === void 0)
                  throw new oe(
                    `Tried to invoke ctor of ${m} with invalid number of parameters (${
                      tt.length
                    }) - expected (${Object.keys(_.constructor_body).toString()}) parameters instead!`,
                  );
                return Qt.apply(this, tt);
              });
              var h = Object.create(I, { constructor: { value: W } });
              W.prototype = h;
              var _ = new Zn(m, W, h, g, O, a, c, p);
              _.baseClass &&
                ((Se = _.baseClass).__derivedClasses ?? (Se.__derivedClasses = []),
                _.baseClass.__derivedClasses.push(_));
              let P = new _e(m, _, !0, !1, !1),
                S = new _e(`${m}*`, _, !1, !1, !1),
                Z = new _e(`${m} const*`, _, !1, !0, !1);
              return (It[e] = { pointerType: S, constPointerType: Z }), kt(E, W), [P, S, Z];
            });
        },
        Ne = (e, t) => {
          let n = [];
          for (let r = 0; r < e; r++) n.push(C[(t + r * 4) >> 2]);
          return n;
        };
      function lr(e) {
        for (let t = 1; t < e.length; ++t) if (e[t] !== null && e[t].destructorFunction === void 0) return !0;
        return !1;
      }
      function Ge(e, t, n, r, o, a) {
        let l = t.length;
        l < 2 && v("argTypes array size mismatch! Must at least get return value and 'this' types!");
        let c = t[1] !== null && n !== null,
          f = lr(t),
          p = t[0].name !== 'void',
          m = l - 2,
          w = new Array(m),
          g = [],
          E = [];
        return me(e, function (...O) {
          O.length !== m && v(`function ${e} called with ${O.length} arguments, expected ${m}`), (E.length = 0);
          let I;
          (g.length = c ? 2 : 1), (g[0] = o), c && ((I = t[1].toWireType(E, this)), (g[1] = I));
          for (let _ = 0; _ < m; ++_) (w[_] = t[_ + 2].toWireType(E, O[_])), g.push(w[_]);
          let W = r(...g);
          function h(_) {
            if (f) We(E);
            else
              for (let P = c ? 1 : 2; P < t.length; P++) {
                let S = P === 1 ? I : w[P - 2];
                t[P].destructorFunction !== null && t[P].destructorFunction(S);
              }
            if (p) return t[0].fromWireType(_);
          }
          return h(W);
        });
      }
      let ur = (e, t, n, r, o, a) => {
          let l = Ne(t, n);
          (o = R(r, o)),
            X([], [e], (c) => {
              c = c[0];
              let f = `constructor ${c.name}`;
              if (
                (c.registeredClass.constructor_body === void 0 && (c.registeredClass.constructor_body = []),
                c.registeredClass.constructor_body[t - 1] !== void 0)
              )
                throw new oe(
                  `Cannot register multiple constructors with identical number of parameters (${t - 1}) for class '${
                    c.name
                  }'! Overload resolution is currently only performed using the parameter count, not actual type info!`,
                );
              return (
                (c.registeredClass.constructor_body[t - 1] = () => {
                  Le(`Cannot construct ${c.name} due to unbound types`, l);
                }),
                X(
                  [],
                  l,
                  (p) => (p.splice(1, 0, null), (c.registeredClass.constructor_body[t - 1] = Ge(f, p, null, o, a)), []),
                ),
                []
              );
            });
        },
        jt = (e) => {
          e = e.trim();
          let t = e.indexOf('(');
          return t !== -1 ? e.substr(0, t) : e;
        },
        dr = (e, t, n, r, o, a, l, c, f) => {
          let p = Ne(n, r);
          (t = L(t)),
            (t = jt(t)),
            (a = R(o, a)),
            X([], [e], (m) => {
              m = m[0];
              let w = `${m.name}.${t}`;
              t.startsWith('@@') && (t = Symbol[t.substring(2)]), c && m.registeredClass.pureVirtualFunctions.push(t);
              function g() {
                Le(`Cannot call ${w} due to unbound types`, p);
              }
              let E = m.registeredClass.instancePrototype,
                M = E[t];
              return (
                M === void 0 || (M.overloadTable === void 0 && M.className !== m.name && M.argCount === n - 2)
                  ? ((g.argCount = n - 2), (g.className = m.name), (E[t] = g))
                  : (St(E, t, w), (E[t].overloadTable[n - 2] = g)),
                X([], p, (O) => {
                  let I = Ge(w, O, m, a, l);
                  return (
                    E[t].overloadTable === void 0
                      ? ((I.argCount = n - 2), (E[t] = I))
                      : (E[t].overloadTable[n - 2] = I),
                    []
                  );
                }),
                []
              );
            });
        },
        Xe = [],
        N = [],
        Je = (e) => {
          e > 9 && --N[e + 1] === 0 && ((N[e] = void 0), Xe.push(e));
        },
        fr = () => N.length / 2 - 5 - Xe.length,
        hr = () => {
          N.push(0, 1, void 0, 1, null, 1, !0, 1, !1, 1), (s.count_emval_handles = fr);
        };
      var Q = {
        toValue: (e) => (e || v(`Cannot use deleted val. handle = ${e}`), N[e]),
        toHandle: (e) => {
          switch (e) {
            case void 0:
              return 2;
            case null:
              return 4;
            case !0:
              return 6;
            case !1:
              return 8;
            default: {
              let t = Xe.pop() || N.length;
              return (N[t] = e), (N[t + 1] = 1), t;
            }
          }
        },
      };
      let pr = {
          name: 'emscripten::val',
          fromWireType: (e) => {
            let t = Q.toValue(e);
            return Je(e), t;
          },
          toWireType: (e, t) => Q.toHandle(t),
          argPackAdvance: V,
          readValueFromPointer: ue,
          destructorFunction: null,
        },
        Ut = (e) => z(e, pr),
        mr = (e, t, n) => {
          switch (t) {
            case 1:
              return n
                ? function (r) {
                    return this.fromWireType(B[r]);
                  }
                : function (r) {
                    return this.fromWireType(F[r]);
                  };
            case 2:
              return n
                ? function (r) {
                    return this.fromWireType(ne[r >> 1]);
                  }
                : function (r) {
                    return this.fromWireType(ce[r >> 1]);
                  };
            case 4:
              return n
                ? function (r) {
                    return this.fromWireType(A[r >> 2]);
                  }
                : function (r) {
                    return this.fromWireType(C[r >> 2]);
                  };
            default:
              throw new TypeError(`invalid integer width (${t}): ${e}`);
          }
        },
        _r = (e, t, n, r) => {
          t = L(t);
          function o() {}
          (o.values = {}),
            z(e, {
              name: t,
              constructor: o,
              fromWireType(a) {
                return this.constructor.values[a];
              },
              toWireType: (a, l) => l.value,
              argPackAdvance: V,
              readValueFromPointer: mr(t, n, r),
              destructorFunction: null,
            }),
            Ve(t, o);
        },
        Ke = (e, t) => {
          let n = q[e];
          return n === void 0 && v(`${t} has unknown type ${Dt(e)}`), n;
        },
        gr = (e, t, n) => {
          let r = Ke(e, 'enum');
          t = L(t);
          let o = r.constructor,
            a = Object.create(r.constructor.prototype, {
              value: { value: n },
              constructor: { value: me(`${r.name}_${t}`, function () {}) },
            });
          (o.values[n] = a), (o[t] = a);
        };
      var qe = (e) => {
        if (e === null) return 'null';
        let t = typeof e;
        return t === 'object' || t === 'array' || t === 'function' ? e.toString() : `${e}`;
      };
      let yr = (e, t) => {
          switch (t) {
            case 4:
              return function (n) {
                return this.fromWireType(ft[n >> 2]);
              };
            case 8:
              return function (n) {
                return this.fromWireType(ht[n >> 3]);
              };
            default:
              throw new TypeError(`invalid float width (${t}): ${e}`);
          }
        },
        vr = (e, t, n) => {
          (t = L(t)),
            z(e, {
              name: t,
              fromWireType: (r) => r,
              toWireType: (r, o) => o,
              argPackAdvance: V,
              readValueFromPointer: yr(t, n),
              destructorFunction: null,
            });
        },
        wr = (e, t, n, r, o, a, l) => {
          let c = Ne(t, n);
          (e = L(e)),
            (e = jt(e)),
            (o = R(r, o)),
            Ve(
              e,
              function () {
                Le(`Cannot call ${e} due to unbound types`, c);
              },
              t - 1,
            ),
            X([], c, (f) => {
              let p = [f[0], null].concat(f.slice(1));
              return kt(e, Ge(e, p, null, o, a), t - 1), [];
            });
        },
        br = (e, t, n) => {
          switch (t) {
            case 1:
              return n ? (r) => B[r] : (r) => F[r];
            case 2:
              return n ? (r) => ne[r >> 1] : (r) => ce[r >> 1];
            case 4:
              return n ? (r) => A[r >> 2] : (r) => C[r >> 2];
            default:
              throw new TypeError(`invalid integer width (${t}): ${e}`);
          }
        },
        Cr = (e, t, n, r, o) => {
          t = L(t);
          let a = (p) => p;
          if (r === 0) {
            let p = 32 - 8 * n;
            a = (m) => (m << p) >>> p;
          }
          let l = t.includes('unsigned'),
            c = (p, m) => {},
            f;
          l
            ? (f = function (p, m) {
                return c(m, this.name), m >>> 0;
              })
            : (f = function (p, m) {
                return c(m, this.name), m;
              }),
            z(e, {
              name: t,
              fromWireType: a,
              toWireType: f,
              argPackAdvance: V,
              readValueFromPointer: br(t, n, r !== 0),
              destructorFunction: null,
            });
        },
        Er = (e, t, n) => {
          let o = [Int8Array, Uint8Array, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array][
            t
          ];
          function a(l) {
            let c = C[l >> 2],
              f = C[(l + 4) >> 2];
            return new o(B.buffer, f, c);
          }
          (n = L(n)),
            z(
              e,
              { name: n, fromWireType: a, argPackAdvance: V, readValueFromPointer: a },
              { ignoreDuplicateRegistrations: !0 },
            );
        },
        Pr = (e, t) => {
          Ut(e);
        },
        $r = (e, t, n, r, o, a, l, c, f, p, m, w) => {
          (n = L(n)),
            (a = R(o, a)),
            (c = R(l, c)),
            (p = R(f, p)),
            (w = R(m, w)),
            X([e], [t], (g) => ((g = g[0]), [new _e(n, g.registeredClass, !1, !1, !0, g, r, a, c, p, w)]));
        },
        Mr = (e, t) => {
          t = L(t);
          let n = t === 'std::string';
          z(e, {
            name: t,
            fromWireType(r) {
              let o = C[r >> 2],
                a = r + 4,
                l;
              if (n) {
                let f = a;
                for (var c = 0; c <= o; ++c) {
                  let p = a + c;
                  if (c == o || F[p] == 0) {
                    let m = p - f,
                      w = K(f, m);
                    l === void 0 ? (l = w) : ((l += '\0'), (l += w)), (f = p + 1);
                  }
                }
              } else {
                let f = new Array(o);
                for (var c = 0; c < o; ++c) f[c] = String.fromCharCode(F[a + c]);
                l = f.join('');
              }
              return G(r), l;
            },
            toWireType(r, o) {
              o instanceof ArrayBuffer && (o = new Uint8Array(o));
              let a,
                l = typeof o == 'string';
              l ||
                o instanceof Uint8Array ||
                o instanceof Uint8ClampedArray ||
                o instanceof Int8Array ||
                v('Cannot pass non-string to std::string'),
                n && l ? (a = Pt(o)) : (a = o.length);
              let c = et(4 + a + 1),
                f = c + 4;
              if (((C[c >> 2] = a), n && l)) Ln(o, f, a + 1);
              else if (l)
                for (var p = 0; p < a; ++p) {
                  let m = o.charCodeAt(p);
                  m > 255 && (G(f), v('String has UTF-16 code units that do not fit in 8 bits')), (F[f + p] = m);
                }
              else for (var p = 0; p < a; ++p) F[f + p] = o[p];
              return r !== null && r.push(G, c), c;
            },
            argPackAdvance: V,
            readValueFromPointer: ue,
            destructorFunction(r) {
              G(r);
            },
          });
        },
        Ot = typeof TextDecoder < 'u' ? new TextDecoder('utf-16le') : void 0,
        Tr = (e, t) => {
          let n = e,
            r = n >> 1,
            o = r + t / 2;
          for (; !(r >= o) && ce[r]; ) ++r;
          if (((n = r << 1), n - e > 32 && Ot)) return Ot.decode(F.subarray(e, n));
          let a = '';
          for (let l = 0; !(l >= t / 2); ++l) {
            let c = ne[(e + l * 2) >> 1];
            if (c == 0) break;
            a += String.fromCharCode(c);
          }
          return a;
        },
        Fr = (e, t, n) => {
          if ((n ?? (n = 2147483647), n < 2)) return 0;
          n -= 2;
          let r = t,
            o = n < e.length * 2 ? n / 2 : e.length;
          for (let a = 0; a < o; ++a) {
            let l = e.charCodeAt(a);
            (ne[t >> 1] = l), (t += 2);
          }
          return (ne[t >> 1] = 0), t - r;
        },
        Lr = (e) => e.length * 2,
        Ar = (e, t) => {
          let n = 0,
            r = '';
          for (; !(n >= t / 4); ) {
            let o = A[(e + n * 4) >> 2];
            if (o == 0) break;
            if ((++n, o >= 65536)) {
              let a = o - 65536;
              r += String.fromCharCode(55296 | (a >> 10), 56320 | (a & 1023));
            } else r += String.fromCharCode(o);
          }
          return r;
        },
        Ir = (e, t, n) => {
          if ((n ?? (n = 2147483647), n < 4)) return 0;
          let r = t,
            o = r + n - 4;
          for (let a = 0; a < e.length; ++a) {
            let l = e.charCodeAt(a);
            if (l >= 55296 && l <= 57343) {
              let c = e.charCodeAt(++a);
              l = (65536 + ((l & 1023) << 10)) | (c & 1023);
            }
            if (((A[t >> 2] = l), (t += 4), t + 4 > o)) break;
          }
          return (A[t >> 2] = 0), t - r;
        },
        Sr = (e) => {
          let t = 0;
          for (let n = 0; n < e.length; ++n) {
            let r = e.charCodeAt(n);
            r >= 55296 && r <= 57343 && ++n, (t += 4);
          }
          return t;
        },
        kr = (e, t, n) => {
          n = L(n);
          let r, o, a, l;
          t === 2
            ? ((r = Tr), (o = Fr), (l = Lr), (a = (c) => ce[c >> 1]))
            : t === 4 && ((r = Ar), (o = Ir), (l = Sr), (a = (c) => C[c >> 2])),
            z(e, {
              name: n,
              fromWireType: (c) => {
                let f = C[c >> 2],
                  p,
                  m = c + 4;
                for (let w = 0; w <= f; ++w) {
                  let g = c + 4 + w * t;
                  if (w == f || a(g) == 0) {
                    let E = g - m,
                      M = r(m, E);
                    p === void 0 ? (p = M) : ((p += '\0'), (p += M)), (m = g + t);
                  }
                }
                return G(c), p;
              },
              toWireType: (c, f) => {
                typeof f != 'string' && v(`Cannot pass non-string to C++ string type ${n}`);
                let p = l(f),
                  m = et(4 + p + t);
                return (C[m >> 2] = p / t), o(f, m + 4, p + t), c !== null && c.push(G, m), m;
              },
              argPackAdvance: V,
              readValueFromPointer: ue,
              destructorFunction(c) {
                G(c);
              },
            });
        },
        Rr = (e, t, n, r, o, a) => {
          Pe[e] = { name: L(t), rawConstructor: R(n, r), rawDestructor: R(o, a), fields: [] };
        },
        xr = (e, t, n, r, o, a, l, c, f, p) => {
          Pe[e].fields.push({
            fieldName: L(t),
            getterReturnType: n,
            getter: R(r, o),
            getterContext: a,
            setterArgumentType: l,
            setter: R(c, f),
            setterContext: p,
          });
        },
        Dr = (e, t) => {
          (t = L(t)),
            z(e, { isVoid: !0, name: t, argPackAdvance: 0, fromWireType: () => {}, toWireType: (n, r) => {} });
        },
        jr = (e, t, n) => F.copyWithin(e, t, t + n),
        Ur = () => {
          throw 1 / 0;
        },
        Qe = [],
        Or = (e, t, n, r) => ((e = Qe[e]), (t = Q.toValue(t)), e(null, t, n, r)),
        Wr = (e) => {
          let t = Qe.length;
          return Qe.push(e), t;
        },
        Br = (e, t) => {
          let n = new Array(e);
          for (let r = 0; r < e; ++r) n[r] = Ke(C[(t + r * 4) >> 2], `parameter ${r}`);
          return n;
        },
        Hr = Reflect.construct,
        zr = (e, t, n) => {
          let r = [],
            o = e.toWireType(r, n);
          return r.length && (C[t >> 2] = Q.toHandle(r)), o;
        },
        Vr = (e, t, n) => {
          let r = Br(e, t),
            o = r.shift();
          e--;
          let a = new Array(e),
            l = (f, p, m, w) => {
              let g = 0;
              for (let M = 0; M < e; ++M) (a[M] = r[M].readValueFromPointer(w + g)), (g += r[M].argPackAdvance);
              let E = n === 1 ? Hr(p, a) : p.apply(f, a);
              return zr(o, m, E);
            },
            c = `methodCaller<(${r.map((f) => f.name).join(', ')}) => ${o.name}>`;
          return Wr(me(c, l));
        },
        Yr = (e) => {
          e > 9 && (N[e + 1] += 1);
        },
        Nr = (e) => {
          let t = Q.toValue(e);
          We(t), Je(e);
        },
        Gr = (e, t) => {
          e = Ke(e, '_emval_take_value');
          let n = e.readValueFromPointer(t);
          return Q.toHandle(n);
        },
        Xr = () => {
          Ce('');
        },
        Wt;
      Wt = () => performance.now();
      let Jr = () => 2147483648,
        Kr = (e) => {
          let t = be.buffer,
            n = (e - t.byteLength + 65535) / 65536;
          try {
            return be.grow(n), pt(), 1;
          } catch {}
        },
        qr = (e) => {
          let t = F.length;
          e >>>= 0;
          let n = Jr();
          if (e > n) return !1;
          let r = (o, a) => o + ((a - (o % a)) % a);
          for (let o = 1; o <= 4; o *= 2) {
            let a = t * (1 + 0.2 / o);
            a = Math.min(a, e + 100663296);
            let l = Math.min(n, r(Math.max(e, a), 65536));
            if (Kr(l)) return !0;
          }
          return !1;
        },
        Ze = {},
        Qr = () => lt || './this.program';
      var ge = () => {
        if (!ge.strings) {
          let n = {
            USER: 'web_user',
            LOGNAME: 'web_user',
            PATH: '/',
            PWD: '/',
            HOME: '/home/web_user',
            LANG: `${((typeof navigator == 'object' && navigator.languages && navigator.languages[0]) || 'C').replace(
              '-',
              '_',
            )}.UTF-8`,
            _: Qr(),
          };
          for (var e in Ze) Ze[e] === void 0 ? delete n[e] : (n[e] = Ze[e]);
          let r = [];
          for (var e in n) r.push(`${e}=${n[e]}`);
          ge.strings = r;
        }
        return ge.strings;
      };
      let Zr = (e, t) => {
          for (let n = 0; n < e.length; ++n) B[t++] = e.charCodeAt(n);
          B[t] = 0;
        },
        ei = (e, t) => {
          let n = 0;
          return (
            ge().forEach((r, o) => {
              let a = t + n;
              (C[(e + o * 4) >> 2] = a), Zr(r, a), (n += r.length + 1);
            }),
            0
          );
        },
        ti = (e, t) => {
          let n = ge();
          C[e >> 2] = n.length;
          let r = 0;
          return n.forEach((o) => (r += o.length + 1)), (C[t >> 2] = r), 0;
        },
        ni = (e) => 52,
        ri = (e, t, n, r) => 52;
      function si(e, t, n, r, o) {
        return 70;
      }
      let oi = [null, [], []],
        ai = (e, t) => {
          let n = oi[e];
          t === 0 || t === 10 ? ((e === 1 ? on : te)(Ct(n, 0)), (n.length = 0)) : n.push(t);
        },
        ci = (e, t, n, r) => {
          let o = 0;
          for (let a = 0; a < n; a++) {
            let l = C[t >> 2],
              c = C[(t + 4) >> 2];
            t += 8;
            for (let f = 0; f < c; f++) ai(e, F[l + f]);
            o += c;
          }
          return (C[r >> 2] = o), 0;
        },
        li = () => {
          if (typeof crypto == 'object' && typeof crypto.getRandomValues == 'function')
            return (e) => crypto.getRandomValues(e);
          Ce('initRandomDevice');
        };
      var Bt = (e) => (Bt = li())(e);
      let ui = (e, t) => (Bt(F.subarray(e, e + t)), 0),
        Ae = (e) => e % 4 === 0 && (e % 100 !== 0 || e % 400 === 0),
        di = (e, t) => {
          let n = 0;
          for (let r = 0; r <= t; n += e[r++]);
          return n;
        },
        Ht = [31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31],
        zt = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31],
        fi = (e, t) => {
          let n = new Date(e.getTime());
          for (; t > 0; ) {
            let r = Ae(n.getFullYear()),
              o = n.getMonth(),
              a = (r ? Ht : zt)[o];
            if (t > a - n.getDate())
              (t -= a - n.getDate() + 1),
                n.setDate(1),
                o < 11 ? n.setMonth(o + 1) : (n.setMonth(0), n.setFullYear(n.getFullYear() + 1));
            else return n.setDate(n.getDate() + t), n;
          }
          return n;
        };
      function hi(e, t, n) {
        let r = n > 0 ? n : Pt(e) + 1,
          o = new Array(r),
          a = $t(e, o, 0, o.length);
        return t && (o.length = a), o;
      }
      let pi = (e, t) => {
          B.set(e, t);
        },
        mi = (e, t, n, r) => {
          let o = C[(r + 40) >> 2],
            a = {
              tm_sec: A[r >> 2],
              tm_min: A[(r + 4) >> 2],
              tm_hour: A[(r + 8) >> 2],
              tm_mday: A[(r + 12) >> 2],
              tm_mon: A[(r + 16) >> 2],
              tm_year: A[(r + 20) >> 2],
              tm_wday: A[(r + 24) >> 2],
              tm_yday: A[(r + 28) >> 2],
              tm_isdst: A[(r + 32) >> 2],
              tm_gmtoff: A[(r + 36) >> 2],
              tm_zone: o ? K(o) : '',
            },
            l = K(n),
            c = {
              '%c': '%a %b %d %H:%M:%S %Y',
              '%D': '%m/%d/%y',
              '%F': '%Y-%m-%d',
              '%h': '%b',
              '%r': '%I:%M:%S %p',
              '%R': '%H:%M',
              '%T': '%H:%M:%S',
              '%x': '%m/%d/%y',
              '%X': '%H:%M:%S',
              '%Ec': '%c',
              '%EC': '%C',
              '%Ex': '%m/%d/%y',
              '%EX': '%H:%M:%S',
              '%Ey': '%y',
              '%EY': '%Y',
              '%Od': '%d',
              '%Oe': '%e',
              '%OH': '%H',
              '%OI': '%I',
              '%Om': '%m',
              '%OM': '%M',
              '%OS': '%S',
              '%Ou': '%u',
              '%OU': '%U',
              '%OV': '%V',
              '%Ow': '%w',
              '%OW': '%W',
              '%Oy': '%y',
            };
          for (var f in c) l = l.replace(new RegExp(f, 'g'), c[f]);
          let p = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'],
            m = [
              'January',
              'February',
              'March',
              'April',
              'May',
              'June',
              'July',
              'August',
              'September',
              'October',
              'November',
              'December',
            ];
          function w(h, _, P) {
            let S = typeof h == 'number' ? h.toString() : h || '';
            for (; S.length < _; ) S = P[0] + S;
            return S;
          }
          function g(h, _) {
            return w(h, _, '0');
          }
          function E(h, _) {
            function P(Z) {
              return Z < 0 ? -1 : Z > 0 ? 1 : 0;
            }
            let S;
            return (
              (S = P(h.getFullYear() - _.getFullYear())) === 0 &&
                (S = P(h.getMonth() - _.getMonth())) === 0 &&
                (S = P(h.getDate() - _.getDate())),
              S
            );
          }
          function M(h) {
            switch (h.getDay()) {
              case 0:
                return new Date(h.getFullYear() - 1, 11, 29);
              case 1:
                return h;
              case 2:
                return new Date(h.getFullYear(), 0, 3);
              case 3:
                return new Date(h.getFullYear(), 0, 2);
              case 4:
                return new Date(h.getFullYear(), 0, 1);
              case 5:
                return new Date(h.getFullYear() - 1, 11, 31);
              case 6:
                return new Date(h.getFullYear() - 1, 11, 30);
            }
          }
          function O(h) {
            let _ = fi(new Date(h.tm_year + 1900, 0, 1), h.tm_yday),
              P = new Date(_.getFullYear(), 0, 4),
              S = new Date(_.getFullYear() + 1, 0, 4),
              Z = M(P),
              Se = M(S);
            return E(Z, _) <= 0 ? (E(Se, _) <= 0 ? _.getFullYear() + 1 : _.getFullYear()) : _.getFullYear() - 1;
          }
          let I = {
            '%a': (h) => p[h.tm_wday].substring(0, 3),
            '%A': (h) => p[h.tm_wday],
            '%b': (h) => m[h.tm_mon].substring(0, 3),
            '%B': (h) => m[h.tm_mon],
            '%C': (h) => {
              let _ = h.tm_year + 1900;
              return g((_ / 100) | 0, 2);
            },
            '%d': (h) => g(h.tm_mday, 2),
            '%e': (h) => w(h.tm_mday, 2, ' '),
            '%g': (h) => O(h).toString().substring(2),
            '%G': O,
            '%H': (h) => g(h.tm_hour, 2),
            '%I': (h) => {
              let _ = h.tm_hour;
              return _ == 0 ? (_ = 12) : _ > 12 && (_ -= 12), g(_, 2);
            },
            '%j': (h) => g(h.tm_mday + di(Ae(h.tm_year + 1900) ? Ht : zt, h.tm_mon - 1), 3),
            '%m': (h) => g(h.tm_mon + 1, 2),
            '%M': (h) => g(h.tm_min, 2),
            '%n': () => `
`,
            '%p': (h) => (h.tm_hour >= 0 && h.tm_hour < 12 ? 'AM' : 'PM'),
            '%S': (h) => g(h.tm_sec, 2),
            '%t': () => '	',
            '%u': (h) => h.tm_wday || 7,
            '%U': (h) => {
              let _ = h.tm_yday + 7 - h.tm_wday;
              return g(Math.floor(_ / 7), 2);
            },
            '%V': (h) => {
              let _ = Math.floor((h.tm_yday + 7 - ((h.tm_wday + 6) % 7)) / 7);
              if (((h.tm_wday + 371 - h.tm_yday - 2) % 7 <= 2 && _++, _)) {
                if (_ == 53) {
                  let P = (h.tm_wday + 371 - h.tm_yday) % 7;
                  P != 4 && (P != 3 || !Ae(h.tm_year)) && (_ = 1);
                }
              } else {
                _ = 52;
                let P = (h.tm_wday + 7 - h.tm_yday - 1) % 7;
                (P == 4 || (P == 5 && Ae((h.tm_year % 400) - 1))) && _++;
              }
              return g(_, 2);
            },
            '%w': (h) => h.tm_wday,
            '%W': (h) => {
              let _ = h.tm_yday + 7 - ((h.tm_wday + 6) % 7);
              return g(Math.floor(_ / 7), 2);
            },
            '%y': (h) => (h.tm_year + 1900).toString().substring(2),
            '%Y': (h) => h.tm_year + 1900,
            '%z': (h) => {
              let _ = h.tm_gmtoff,
                P = _ >= 0;
              return (_ = Math.abs(_) / 60), (_ = (_ / 60) * 100 + (_ % 60)), (P ? '+' : '-') + `0000${_}`.slice(-4);
            },
            '%Z': (h) => h.tm_zone,
            '%%': () => '%',
          };
          l = l.replace(/%%/g, '\0\0');
          for (var f in I) l.includes(f) && (l = l.replace(new RegExp(f, 'g'), I[f](a)));
          l = l.replace(/\0\0/g, '%');
          let W = hi(l, !1);
          return W.length > t ? 0 : (pi(W, e), W.length - 1);
        },
        _i = (e, t, n, r, o) => mi(e, t, n, r);
      (Mt = s.InternalError =
        class extends Error {
          constructor(t) {
            super(t), (this.name = 'InternalError');
          }
        }),
        jn(),
        (oe = s.BindingError =
          class extends Error {
            constructor(t) {
              super(t), (this.name = 'BindingError');
            }
          }),
        Jn(),
        Yn(),
        rr(),
        (xt = s.UnboundTypeError = ar(Error, 'UnboundTypeError')),
        hr();
      var gi = {
          c: bn,
          d: Pn,
          q: Mn,
          h: Cn,
          F: Tn,
          V: Fn,
          S: An,
          W: In,
          T: Sn,
          E: kn,
          U: Rn,
          A: xn,
          N: Dn,
          da: On,
          v: cr,
          u: ur,
          i: dr,
          ca: Ut,
          C: _r,
          n: gr,
          I: vr,
          K: wr,
          p: Cr,
          k: Er,
          B: Pr,
          ja: $r,
          J: Mr,
          z: kr,
          w: Rr,
          m: xr,
          ea: Dr,
          X: jr,
          O: Ur,
          ha: Or,
          P: Je,
          ga: Vr,
          ia: Yr,
          fa: Nr,
          r: Gr,
          ba: Xr,
          l: Wt,
          R: qr,
          Y: ei,
          Z: ti,
          y: ni,
          D: ri,
          M: si,
          x: ci,
          _: ui,
          j: ji,
          g: Vi,
          f: zi,
          aa: Ri,
          s: Hi,
          G: Wi,
          t: Oi,
          b: ki,
          a: xi,
          e: Di,
          o: Bi,
          H: Ui,
          $: Yi,
          L: Ni,
          Q: _i,
        },
        b = wn(),
        et = (e) => (et = b.ma)(e),
        Vt = (e) => (Vt = b.na)(e),
        G = (e) => (G = b.pa)(e),
        x = (e, t) => (x = b.qa)(e, t),
        Yt = (e) => (Yt = b.ra)(e),
        Nt = (e) => (Nt = b.sa)(e),
        Gt = () => (Gt = b.ta)(),
        Xt = (e, t, n) => (Xt = b.ua)(e, t, n),
        Jt = (e) => (Jt = b.va)(e);
      s.dynCall_iijj = (e, t, n, r, o, a) => (s.dynCall_iijj = b.wa)(e, t, n, r, o, a);
      s.dynCall_vijj = (e, t, n, r, o, a) => (s.dynCall_vijj = b.xa)(e, t, n, r, o, a);
      s.dynCall_jiii = (e, t, n, r) => (s.dynCall_jiii = b.ya)(e, t, n, r);
      s.dynCall_jii = (e, t, n) => (s.dynCall_jii = b.za)(e, t, n);
      var Kt = (s.dynCall_viiij = (e, t, n, r, o, a) => (Kt = s.dynCall_viiij = b.Aa)(e, t, n, r, o, a));
      s.dynCall_jiji = (e, t, n, r, o) => (s.dynCall_jiji = b.Ba)(e, t, n, r, o);
      s.dynCall_viijii = (e, t, n, r, o, a, l) => (s.dynCall_viijii = b.Ca)(e, t, n, r, o, a, l);
      s.dynCall_iiiiij = (e, t, n, r, o, a, l) => (s.dynCall_iiiiij = b.Da)(e, t, n, r, o, a, l);
      s.dynCall_iiiiijj = (e, t, n, r, o, a, l, c, f) => (s.dynCall_iiiiijj = b.Ea)(e, t, n, r, o, a, l, c, f);
      s.dynCall_iiiiiijj = (e, t, n, r, o, a, l, c, f, p) => (s.dynCall_iiiiiijj = b.Fa)(e, t, n, r, o, a, l, c, f, p);
      function ki(e, t) {
        let n = U();
        try {
          k(e)(t);
        } catch (r) {
          if ((j(n), r !== r + 0)) throw r;
          x(1, 0);
        }
      }
      function Ri(e, t, n, r, o) {
        let a = U();
        try {
          return k(e)(t, n, r, o);
        } catch (l) {
          if ((j(a), l !== l + 0)) throw l;
          x(1, 0);
        }
      }
      function xi(e, t, n) {
        let r = U();
        try {
          k(e)(t, n);
        } catch (o) {
          if ((j(r), o !== o + 0)) throw o;
          x(1, 0);
        }
      }
      function Di(e, t, n, r) {
        let o = U();
        try {
          k(e)(t, n, r);
        } catch (a) {
          if ((j(o), a !== a + 0)) throw a;
          x(1, 0);
        }
      }
      function ji(e, t) {
        let n = U();
        try {
          return k(e)(t);
        } catch (r) {
          if ((j(n), r !== r + 0)) throw r;
          x(1, 0);
        }
      }
      function Ui(e, t, n, r, o, a) {
        let l = U();
        try {
          k(e)(t, n, r, o, a);
        } catch (c) {
          if ((j(l), c !== c + 0)) throw c;
          x(1, 0);
        }
      }
      function Oi(e) {
        let t = U();
        try {
          k(e)();
        } catch (n) {
          if ((j(t), n !== n + 0)) throw n;
          x(1, 0);
        }
      }
      function Wi(e, t, n, r, o, a, l, c) {
        let f = U();
        try {
          return k(e)(t, n, r, o, a, l, c);
        } catch (p) {
          if ((j(f), p !== p + 0)) throw p;
          x(1, 0);
        }
      }
      function Bi(e, t, n, r, o) {
        let a = U();
        try {
          k(e)(t, n, r, o);
        } catch (l) {
          if ((j(a), l !== l + 0)) throw l;
          x(1, 0);
        }
      }
      function Hi(e, t, n, r, o, a) {
        let l = U();
        try {
          return k(e)(t, n, r, o, a);
        } catch (c) {
          if ((j(l), c !== c + 0)) throw c;
          x(1, 0);
        }
      }
      function zi(e, t, n, r) {
        let o = U();
        try {
          return k(e)(t, n, r);
        } catch (a) {
          if ((j(o), a !== a + 0)) throw a;
          x(1, 0);
        }
      }
      function Vi(e, t, n) {
        var r = U();
        try {
          return k(e)(t, n);
        } catch (o) {
          if ((j(r), o !== o + 0)) throw o;
          x(1, 0);
        }
      }
      function Yi(e, t, n, r, o, a, l) {
        let c = U();
        try {
          k(e)(t, n, r, o, a, l);
        } catch (f) {
          if ((j(c), f !== f + 0)) throw f;
          x(1, 0);
        }
      }
      function Ni(e, t, n, r, o, a) {
        let l = U();
        try {
          Kt(e, t, n, r, o, a);
        } catch (c) {
          if ((j(l), c !== c + 0)) throw c;
          x(1, 0);
        }
      }
      let Ie;
      le = function e() {
        Ie || qt(), Ie || (le = e);
      };
      function qt() {
        if (J > 0 || (ln(), J > 0)) return;
        function e() {
          Ie ||
            ((Ie = !0),
            (s.calledRun = !0),
            !dt && (un(), d(s), s.onRuntimeInitialized && s.onRuntimeInitialized(), dn()));
        }
        s.setStatus
          ? (s.setStatus('Running...'),
            setTimeout(function () {
              setTimeout(function () {
                s.setStatus('');
              }, 1),
                e();
            }, 1))
          : e();
      }
      if (s.preInit)
        for (typeof s.preInit == 'function' && (s.preInit = [s.preInit]); s.preInit.length > 0; ) s.preInit.pop()();
      return qt(), D;
    };
  })(),
  Zt = Ki;
var ee = class {
  constructor() {
    throw new Error('RendererLoader is a static class and cannot be instantiated.');
  }
  static async _tryLoad(i) {
    return await Zt({ locateFile: () => i });
  }
  static async _loadWithBackup() {
    return (
      this._ModulePromise ||
        (this._ModulePromise = this._tryLoad(this._wasmURL).catch(async (i) => {
          let s = `https://unpkg.com/${ye.name}@${ye.version}/dist/dotlottie-player.wasm`;
          console.warn(`Primary WASM load failed from ${this._wasmURL}. Error: ${i.message}`),
            console.warn(`Attempting to load WASM from backup URL: ${s}`);
          try {
            return await this._tryLoad(s);
          } catch (d) {
            throw (
              (console.error(`Primary WASM URL failed: ${i.message}`),
              console.error(`Backup WASM URL failed: ${d.message}`),
              new Error('WASM loading failed from all sources.'))
            );
          }
        })),
      this._ModulePromise
    );
  }
  static async load() {
    return this._loadWithBackup();
  }
  static setWasmUrl(i) {
    (this._wasmURL = i), (this._ModulePromise = null);
  }
};
T(ee, '_ModulePromise', null),
  T(ee, '_wasmURL', `https://cdn.jsdelivr.net/npm/${ye.name}@${ye.version}/dist/dotlottie-player.wasm`);
var Re = class {
  constructor() {
    T(this, '_eventListeners', new Map());
  }
  addEventListener(i, s) {
    let d = this._eventListeners.get(i);
    d || ((d = new Set()), this._eventListeners.set(i, d)), d.add(s);
  }
  removeEventListener(i, s) {
    let d = this._eventListeners.get(i);
    d && (s ? (d.delete(s), d.size === 0 && this._eventListeners.delete(i)) : this._eventListeners.delete(i));
  }
  dispatch(i) {
    this._eventListeners.get(i.type)?.forEach((d) => d(i));
  }
  removeAllEventListeners() {
    this._eventListeners.clear();
  }
};
var st = (u, i) =>
    u === 'reverse'
      ? i.Mode.Reverse
      : u === 'bounce'
      ? i.Mode.Bounce
      : u === 'reverse-bounce'
      ? i.Mode.ReverseBounce
      : i.Mode.Forward,
  ot = (u, i) =>
    u === 'contain'
      ? i.Fit.Contain
      : u === 'cover'
      ? i.Fit.Cover
      : u === 'fill'
      ? i.Fit.Fill
      : u === 'fit-height'
      ? i.Fit.FitHeight
      : u === 'fit-width'
      ? i.Fit.FitWidth
      : i.Fit.None,
  at = (u, i) => {
    let s = new i.VectorFloat();
    return s.push_back(u[0]), s.push_back(u[1]), s;
  },
  ct = (u, i) => {
    let s = new i.VectorFloat();
    return u.length !== 2 || (s.push_back(u[0]), s.push_back(u[1])), s;
  },
  ve = class {
    constructor(i) {
      T(this, '_canvas');
      T(this, '_context', null);
      T(this, '_eventManager');
      T(this, '_animationFrameId', null);
      T(this, '_frameManager');
      T(this, '_dotLottieCore', null);
      T(this, '_wasmModule', null);
      T(this, '_renderConfig', {});
      T(this, '_isFrozen', !1);
      T(this, '_backgroundColor', null);
      (this._canvas = i.canvas),
        (this._context = this._canvas.getContext('2d')),
        (this._eventManager = new Re()),
        (this._frameManager = new ke()),
        (this._renderConfig = i.renderConfig ?? {}),
        ee
          .load()
          .then((s) => {
            (this._wasmModule = s),
              (this._dotLottieCore = new s.DotLottiePlayer({
                autoplay: i.autoplay ?? !1,
                backgroundColor: 0,
                loopAnimation: i.loop ?? !1,
                mode: st(i.mode ?? 'forward', s),
                segment: ct(i.segment ?? [], s),
                speed: i.speed ?? 1,
                useFrameInterpolation: i.useFrameInterpolation ?? !0,
                marker: i.marker ?? '',
                layout: i.layout ? { align: at(i.layout.align, s), fit: ot(i.layout.fit, s) } : s.createDefaultLayout(),
              })),
              i.data ? this._loadFromData(i.data) : i.src && this._loadFromSrc(i.src),
              i.backgroundColor && this.setBackgroundColor(i.backgroundColor);
          })
          .catch((s) => {
            this._eventManager.dispatch({ type: 'loadError', error: new Error(`Failed to load wasm module: ${s}`) });
          });
    }
    _loadFromSrc(i) {
      async function s() {
        let d = await fetch(i);
        if (!d.ok) throw new Error(`Failed to fetch the animation data from URL: ${i}. ${d.status}: ${d.statusText}`);
        let y = (d.headers.get('content-type') ?? '').trim(),
          D;
        return (
          ['application/json', 'text/plain'].some((Y) => y.startsWith(Y))
            ? (D = await d.text())
            : (D = await d.arrayBuffer()),
          D
        );
      }
      s()
        .then((d) => {
          this._loadFromData(d);
        })
        .catch((d) => {
          this._eventManager.dispatch({
            type: 'loadError',
            error: new Error(`Failed to load animation data from URL: ${i}. ${d}`),
          });
        });
    }
    _loadFromData(i) {
      if (this._dotLottieCore === null) return;
      let s = this._canvas.width,
        d = this._canvas.height,
        y = !1;
      if (typeof i == 'string') y = this._dotLottieCore.loadAnimationData(i, s, d);
      else if (i instanceof ArrayBuffer) y = this._dotLottieCore.loadDotLottieData(i, s, d);
      else if (typeof i == 'object') y = this._dotLottieCore.loadAnimationData(JSON.stringify(i), s, d);
      else {
        this._eventManager.dispatch({
          type: 'loadError',
          error: new Error('Unsupported data type for animation data. Expected a string or ArrayBuffer.'),
        });
        return;
      }
      y
        ? (this._eventManager.dispatch({ type: 'load' }),
          it && this.resize(),
          this._eventManager.dispatch({ type: 'frame', currentFrame: this._dotLottieCore.currentFrame() }),
          this._render(),
          this._dotLottieCore.config().autoplay &&
            (this._dotLottieCore.play(),
            this._dotLottieCore.isPlaying()
              ? (this._eventManager.dispatch({ type: 'play' }),
                (this._animationFrameId = this._frameManager.requestAnimationFrame(this._draw.bind(this))))
              : console.error('something went wrong, the animation was suppose to autoplay')))
        : this._eventManager.dispatch({ type: 'loadError', error: new Error('Failed to load animation data') });
    }
    get activeAnimationId() {
      return this._dotLottieCore?.activeAnimationId();
    }
    get activeThemeId() {
      return this._dotLottieCore?.activeThemeId();
    }
    get layout() {
      let i = this._dotLottieCore?.config().layout;
      if (i)
        return {
          align: [i.align.get(0), i.align.get(1)],
          fit: (() => {
            switch (i.fit) {
              case this._wasmModule?.Fit.Contain:
                return 'contain';
              case this._wasmModule?.Fit.Cover:
                return 'cover';
              case this._wasmModule?.Fit.Fill:
                return 'fill';
              case this._wasmModule?.Fit.FitHeight:
                return 'fit-height';
              case this._wasmModule?.Fit.FitWidth:
                return 'fit-width';
              case this._wasmModule?.Fit.None:
                return 'none';
              default:
                return 'contain';
            }
          })(),
        };
    }
    get marker() {
      return this._dotLottieCore?.config().marker;
    }
    get manifest() {
      try {
        let i = this._dotLottieCore?.manifestString();
        if (this._dotLottieCore === null || !i) return null;
        let s = JSON.parse(i);
        return Object.keys(s).length === 0 ? null : s;
      } catch {
        return null;
      }
    }
    get renderConfig() {
      return this._renderConfig;
    }
    get segment() {
      let i = this._dotLottieCore?.config().segment;
      if (i && i.size() === 2) return [i.get(0), i.get(1)];
    }
    get loop() {
      return this._dotLottieCore?.config().loopAnimation ?? !1;
    }
    get mode() {
      let i = this._dotLottieCore?.config().mode;
      return i === this._wasmModule?.Mode.Reverse
        ? 'reverse'
        : i === this._wasmModule?.Mode.Bounce
        ? 'bounce'
        : i === this._wasmModule?.Mode.ReverseBounce
        ? 'reverse-bounce'
        : 'forward';
    }
    get isFrozen() {
      return this._isFrozen;
    }
    get backgroundColor() {
      return this._backgroundColor ?? '';
    }
    get autoplay() {
      return this._dotLottieCore?.config().autoplay ?? !1;
    }
    get useFrameInterpolation() {
      return this._dotLottieCore?.config().useFrameInterpolation ?? !1;
    }
    get speed() {
      return this._dotLottieCore?.config().speed ?? 0;
    }
    get isLoaded() {
      return this._dotLottieCore?.isLoaded() ?? !1;
    }
    get isPlaying() {
      return this._dotLottieCore?.isPlaying() ?? !1;
    }
    get isPaused() {
      return this._dotLottieCore?.isPaused() ?? !1;
    }
    get isStopped() {
      return this._dotLottieCore?.isStopped() ?? !1;
    }
    get currentFrame() {
      return this._dotLottieCore?.currentFrame() ?? 0;
    }
    get loopCount() {
      return this._dotLottieCore?.loopCount() ?? 0;
    }
    get totalFrames() {
      return this._dotLottieCore?.totalFrames() ?? 0;
    }
    get duration() {
      return this._dotLottieCore?.duration() ?? 0;
    }
    get segmentDuration() {
      return this._dotLottieCore?.segmentDuration() ?? 0;
    }
    get canvas() {
      return this._canvas;
    }
    load(i) {
      this._dotLottieCore === null ||
        this._wasmModule === null ||
        (this._dotLottieCore.setConfig({
          autoplay: i.autoplay ?? !1,
          backgroundColor: 0,
          loopAnimation: i.loop ?? !1,
          mode: st(i.mode ?? 'forward', this._wasmModule),
          segment: ct(i.segment ?? [], this._wasmModule),
          speed: i.speed ?? 1,
          useFrameInterpolation: i.useFrameInterpolation ?? !0,
          marker: i.marker ?? '',
          layout: i.layout
            ? { align: at(i.layout.align, this._wasmModule), fit: ot(i.layout.fit, this._wasmModule) }
            : this._wasmModule.createDefaultLayout(),
        }),
        i.data ? this._loadFromData(i.data) : i.src && this._loadFromSrc(i.src),
        this.setBackgroundColor(i.backgroundColor ?? ''));
    }
    _render() {
      if (this._dotLottieCore === null || this._context === null) return !1;
      if (this._dotLottieCore.render()) {
        let s = this._dotLottieCore.buffer(),
          d = new Uint8ClampedArray(s, 0, this._canvas.width * this._canvas.height * 4),
          y = null;
        return (
          typeof ImageData > 'u'
            ? ((y = this._context.createImageData(this._canvas.width, this._canvas.height)), y.data.set(d))
            : (y = new ImageData(d, this._canvas.width, this._canvas.height)),
          this._context.putImageData(y, 0, 0),
          this._eventManager.dispatch({ type: 'render', currentFrame: this._dotLottieCore.currentFrame() }),
          !0
        );
      }
      return !1;
    }
    _draw() {
      if (this._dotLottieCore === null || this._context === null || !this._dotLottieCore.isPlaying()) return;
      let i = this._dotLottieCore.requestFrame();
      this._dotLottieCore.setFrame(i) &&
        (this._eventManager.dispatch({ type: 'frame', currentFrame: this._dotLottieCore.currentFrame() }),
        this._render() &&
          this._dotLottieCore.isComplete() &&
          (this._dotLottieCore.config().loopAnimation
            ? this._eventManager.dispatch({ type: 'loop', loopCount: this._dotLottieCore.loopCount() })
            : this._eventManager.dispatch({ type: 'complete' }))),
        (this._animationFrameId = this._frameManager.requestAnimationFrame(this._draw.bind(this)));
    }
    play() {
      if (this._dotLottieCore === null) return;
      let i = this._dotLottieCore.play();
      (this._isFrozen = !1),
        i &&
          (this._eventManager.dispatch({ type: 'play' }),
          (this._animationFrameId = this._frameManager.requestAnimationFrame(this._draw.bind(this))));
    }
    pause() {
      if (this._dotLottieCore === null) return;
      this._dotLottieCore.pause() && this._eventManager.dispatch({ type: 'pause' });
    }
    stop() {
      if (this._dotLottieCore === null) return;
      this._dotLottieCore.stop() &&
        (this._eventManager.dispatch({ type: 'frame', currentFrame: this._dotLottieCore.currentFrame() }),
        this._render(),
        this._eventManager.dispatch({ type: 'stop' }));
    }
    setFrame(i) {
      if (this._dotLottieCore === null || i < 0 || i > this._dotLottieCore.totalFrames()) return;
      this._dotLottieCore.seek(i) &&
        (this._eventManager.dispatch({ type: 'frame', currentFrame: this._dotLottieCore.currentFrame() }),
        this._render());
    }
    setSpeed(i) {
      this._dotLottieCore !== null && this._dotLottieCore.setConfig({ ...this._dotLottieCore.config(), speed: i });
    }
    setBackgroundColor(i) {
      this._dotLottieCore !== null &&
        (this._canvas instanceof HTMLCanvasElement && (this._canvas.style.backgroundColor = i),
        (this._backgroundColor = i));
    }
    setLoop(i) {
      this._dotLottieCore !== null &&
        this._dotLottieCore.setConfig({ ...this._dotLottieCore.config(), loopAnimation: i });
    }
    setUseFrameInterpolation(i) {
      this._dotLottieCore !== null &&
        this._dotLottieCore.setConfig({ ...this._dotLottieCore.config(), useFrameInterpolation: i });
    }
    addEventListener(i, s) {
      this._eventManager.addEventListener(i, s);
    }
    removeEventListener(i, s) {
      this._eventManager.removeEventListener(i, s);
    }
    destroy() {
      this._dotLottieCore?.delete(),
        (this._dotLottieCore = null),
        (this._context = null),
        this._eventManager.dispatch({ type: 'destroy' }),
        this._eventManager.removeAllEventListeners(),
        this._cleanupStateMachineListeners();
    }
    freeze() {
      this._animationFrameId !== null &&
        (this._frameManager.cancelAnimationFrame(this._animationFrameId),
        (this._animationFrameId = null),
        (this._isFrozen = !0),
        this._eventManager.dispatch({ type: 'freeze' }));
    }
    unfreeze() {
      this._animationFrameId === null &&
        ((this._animationFrameId = this._frameManager.requestAnimationFrame(this._draw.bind(this))),
        (this._isFrozen = !1),
        this._eventManager.dispatch({ type: 'unfreeze' }));
    }
    resize() {
      if (it && this._canvas instanceof HTMLCanvasElement) {
        let s = this._renderConfig.devicePixelRatio || window.devicePixelRatio || 1,
          { height: d, width: y } = this._canvas.getBoundingClientRect();
        (this._canvas.width = y * s), (this._canvas.height = d * s);
      }
      this._dotLottieCore?.resize(this._canvas.width, this._canvas.height) && this._render();
    }
    setSegment(i, s) {
      this._dotLottieCore === null ||
        this._wasmModule === null ||
        this._dotLottieCore.setConfig({ ...this._dotLottieCore.config(), segment: ct([i, s], this._wasmModule) });
    }
    setMode(i) {
      this._dotLottieCore === null ||
        this._wasmModule === null ||
        this._dotLottieCore.setConfig({ ...this._dotLottieCore.config(), mode: st(i, this._wasmModule) });
    }
    setRenderConfig(i) {
      this._renderConfig = i;
    }
    loadAnimation(i) {
      if (this._dotLottieCore === null || this._dotLottieCore.activeAnimationId() === i) return;
      this._dotLottieCore.loadAnimation(i, this._canvas.width, this._canvas.height)
        ? (this._eventManager.dispatch({ type: 'load' }), this.resize())
        : this._eventManager.dispatch({ type: 'loadError', error: new Error(`Failed to animation :${i}`) });
    }
    setMarker(i) {
      this._dotLottieCore !== null && this._dotLottieCore.setConfig({ ...this._dotLottieCore.config(), marker: i });
    }
    markers() {
      let i = this._dotLottieCore?.markers();
      if (i) {
        let s = [];
        for (let d = 0; d < i.size(); d += 1) {
          let y = i.get(d);
          s.push({ name: y.name, time: y.time, duration: y.duration });
        }
        return s;
      }
      return [];
    }
    loadTheme(i) {
      if (this._dotLottieCore === null) return !1;
      let s = this._dotLottieCore.loadTheme(i);
      return this._render(), s;
    }
    loadThemeData(i) {
      if (this._dotLottieCore === null) return !1;
      let s = this._dotLottieCore.loadThemeData(i);
      return this._render(), s;
    }
    setLayout(i) {
      this._dotLottieCore === null ||
        this._wasmModule === null ||
        this._dotLottieCore.setConfig({
          ...this._dotLottieCore.config(),
          layout: { align: at(i.align, this._wasmModule), fit: ot(i.fit, this._wasmModule) },
        });
    }
    setViewport(i, s, d, y) {
      return this._dotLottieCore === null ? !1 : this._dotLottieCore.setViewport(i, s, d, y);
    }
    static setWasmUrl(i) {
      ee.setWasmUrl(i);
    }
    loadStateMachine(i) {
      return this._dotLottieCore?.loadStateMachine(i) ?? !1;
    }
    startStateMachine() {
      let i = this._dotLottieCore?.startStateMachine() ?? !1;
      return i && this._setupStateMachineListeners(), i;
    }
    stopStateMachine() {
      let i = this._dotLottieCore?.stopStateMachine() ?? !1;
      return i && this._cleanupStateMachineListeners(), i;
    }
    _getPointerPosition(i) {
      let s = this._canvas.getBoundingClientRect(),
        d = this._canvas.width / s.width,
        y = this._canvas.height / s.height,
        D = this._renderConfig.devicePixelRatio || window.devicePixelRatio || 1,
        Y = ((i.clientX - s.left) * d) / D,
        xe = ((i.clientY - s.top) * y) / D;
      return { x: Y, y: xe };
    }
    _onPointerUp(i) {
      let { x: s, y: d } = this._getPointerPosition(i);
      this.postStateMachineEvent(`OnPointerUp: ${s} ${d}`);
    }
    _onPointerDown(i) {
      let { x: s, y: d } = this._getPointerPosition(i);
      this.postStateMachineEvent(`OnPointerDown: ${s} ${d}`);
    }
    _onPointerMove(i) {
      let { x: s, y: d } = this._getPointerPosition(i);
      this.postStateMachineEvent(`OnPointerMove: ${s} ${d}`);
    }
    _onPointerEnter(i) {
      let { x: s, y: d } = this._getPointerPosition(i);
      this.postStateMachineEvent(`OnPointerEnter: ${s} ${d}`);
    }
    _onPointerLeave(i) {
      let { x: s, y: d } = this._getPointerPosition(i);
      this.postStateMachineEvent(`OnPointerExit: ${s} ${d}`);
    }
    _onComplete() {
      this.postStateMachineEvent('OnComplete');
    }
    postStateMachineEvent(i) {
      return this._dotLottieCore?.postEventPayload(i) ?? !1;
    }
    _setupStateMachineListeners() {
      if (this._canvas instanceof HTMLCanvasElement && this._dotLottieCore !== null && this.isLoaded) {
        let i = this._dotLottieCore.stateMachineFrameworkSetup(),
          s = [];
        for (let d = 0; d < i.size(); d += 1) s.push(i.get(d));
        s.includes('PointerUp') && this._canvas.addEventListener('pointerup', this._onPointerUp.bind(this)),
          s.includes('PointerDown') && this._canvas.addEventListener('pointerdown', this._onPointerDown.bind(this)),
          s.includes('PointerMove') && this._canvas.addEventListener('pointermove', this._onPointerMove.bind(this)),
          s.includes('PointerEnter') && this._canvas.addEventListener('pointerenter', this._onPointerEnter.bind(this)),
          s.includes('PointerExit') && this._canvas.addEventListener('pointerleave', this._onPointerLeave.bind(this)),
          s.includes('Complete') && this.addEventListener('complete', this._onComplete.bind(this));
      }
    }
    _cleanupStateMachineListeners() {
      this._canvas instanceof HTMLCanvasElement &&
        (this._canvas.removeEventListener('pointerup', this._onPointerUp.bind(this)),
        this._canvas.removeEventListener('pointerdown', this._onPointerDown.bind(this)),
        this._canvas.removeEventListener('pointermove', this._onPointerMove.bind(this)),
        this._canvas.removeEventListener('pointerenter', this._onPointerEnter.bind(this)),
        this._canvas.removeEventListener('pointerleave', this._onPointerLeave.bind(this)),
        this.removeEventListener('complete', this._onComplete.bind(this)));
    }
  };
var $ = new Map(),
  en = {
    create: (u) => {
      let i = u.params.instanceId,
        s = u.params.config,
        d = u.params.width,
        y = u.params.height;
      if ($.has(i)) throw new Error(`Instance with id ${i} already exists.`);
      let D = new ve(s);
      return (D.canvas.height = y), (D.canvas.width = d), $.set(i, D), { instanceId: i, success: !0 };
    },
    destroy: (u) => {
      let i = u.params.instanceId,
        s = $.get(i);
      if (!s) throw new Error(`Instance with id ${i} does not exist.`);
      return s.destroy(), $.delete(i), { success: !0 };
    },
    freeze: (u) => {
      let i = u.params.instanceId,
        s = $.get(i);
      if (!s) throw new Error(`Instance with id ${i} does not exist.`);
      return s.freeze(), { success: !0 };
    },
    load: (u) => {
      let i = u.params.instanceId,
        s = u.params.config;
      if (!$.has(i)) throw new Error(`Instance with id ${i} does not exist.`);
      return $.get(i)?.load(s), { success: !0 };
    },
    loadAnimation: (u) => {
      let i = u.params.instanceId,
        s = u.params.animationId,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.loadAnimation(s), { success: !0 };
    },
    loadTheme: (u) => {
      let i = u.params.instanceId,
        s = u.params.themeId,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.loadTheme(s), { success: !0 };
    },
    loadThemeData: (u) => {
      let i = u.params.instanceId,
        s = u.params.themeData,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.loadThemeData(s), { success: !0 };
    },
    pause: (u) => {
      let i = u.params.instanceId,
        s = $.get(i);
      if (!s) throw new Error(`Instance with id ${i} does not exist.`);
      return s.pause(), { success: !0 };
    },
    play: (u) => {
      let i = u.params.instanceId,
        s = $.get(i);
      if (!s) throw new Error(`Instance with id ${i} does not exist.`);
      return s.play(), { success: !0 };
    },
    resize: (u) => {
      let i = u.params.instanceId,
        s = u.params.width,
        d = u.params.height,
        y = $.get(i);
      if (!y) throw new Error(`Instance with id ${i} does not exist.`);
      return (y.canvas.height = d), (y.canvas.width = s), y.resize(), { success: !0 };
    },
    setBackgroundColor: (u) => {
      let i = u.params.instanceId,
        s = u.params.backgroundColor,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setBackgroundColor(s), { success: !0 };
    },
    setFrame: (u) => {
      let i = u.params.instanceId,
        s = u.params.frame,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setFrame(s), { success: !0 };
    },
    setMode: (u) => {
      let i = u.params.instanceId,
        s = u.params.mode,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setMode(s), { success: !0 };
    },
    setRenderConfig: (u) => {
      let i = u.params.instanceId,
        s = u.params.renderConfig,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setRenderConfig(s), { success: !0 };
    },
    setSegment: (u) => {
      let i = u.params.instanceId,
        s = u.params.segment,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setSegment(s[0], s[1]), { success: !0 };
    },
    setSpeed: (u) => {
      let i = u.params.instanceId,
        s = u.params.speed,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setSpeed(s), { success: !0 };
    },
    setUseFrameInterpolation: (u) => {
      let i = u.params.instanceId,
        s = u.params.useFrameInterpolation,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setUseFrameInterpolation(s), { success: !0 };
    },
    setWasmUrl: (u) => (ve.setWasmUrl(u.params.wasmUrl), { success: !0 }),
    stop: (u) => {
      let i = u.params.instanceId,
        s = $.get(i);
      if (!s) throw new Error(`Instance with id ${i} does not exist.`);
      return s.stop(), { success: !0 };
    },
    unfreeze: (u) => {
      let i = u.params.instanceId,
        s = $.get(i);
      if (!s) throw new Error(`Instance with id ${i} does not exist.`);
      return s.unfreeze(), { success: !0 };
    },
    setViewport(u) {
      let i = u.params.instanceId,
        s = u.params.x,
        d = u.params.y,
        y = u.params.width,
        D = u.params.height,
        Y = $.get(i);
      if (!Y) throw new Error(`Instance with id ${i} does not exist.`);
      return { success: Y.setViewport(s, d, y, D) };
    },
    setMarker(u) {
      let i = u.params.instanceId,
        s = u.params.marker,
        d = $.get(i);
      if (!d) throw new Error(`Instance with id ${i} does not exist.`);
      return d.setMarker(s), { success: !0 };
    },
  };
function qi(u) {
  let i = u.method;
  if (typeof en[i] == 'function') return en[i](u);
  throw new Error(`Method ${i} is not implemented in commands.`);
}
self.onmessage = (u) => {
  try {
    let i = qi(u.data),
      s = { id: u.data.id, method: u.data.method, result: i };
    self.postMessage(s);
  } catch (i) {
    let s = { id: u.data.id, method: u.data.method, error: { message: i.message } };
    self.postMessage(s);
  }
};
var Qi = '',
  Es = Qi;

export { Es as default };
